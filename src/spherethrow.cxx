#include <iostream>
#include <string>
#include <queue>
#include <vector>                

using namespace std;
int main(int argc, const char** argv)
{                   
	string input_line;
	while(cin) {
		getline(cin, input_line);
		cout << input_line << endl;
	}
	return 0;
}